[pre_model_sync]

[post_model_sync]
# FIRST. A legacy `kind` in one approval-stage row fails validation for the
# WHOLE Settings document, so every patch below that saves Settings dies on it --
# migrate_farms_and_approvers, move_farms_to_upande_core and
# merge_farms_in_use_into_farms all do, and so does seed_stages() at
# after_migrate. Nothing here depends on anything, so first is the only position
# that is right whatever gets added later.
work_management.patches.v1_0.normalize_legacy_stage_kinds
work_management.patches.v1_0.migrate_farms_and_approvers
work_management.patches.v1_0.backfill_farms_in_use
work_management.patches.v1_0.seed_sections_from_cost_centres
# last of the farm patches: the three above all no-op once the doctype below
# is gone, so this one runs after them
work_management.patches.v1_0.move_farms_to_upande_core
work_management.patches.v1_0.backfill_task_subjects
work_management.patches.v1_0.link_planners_to_their_master_plan
work_management.patches.v1_0.merge_farms_in_use_into_farms
work_management.patches.v1_0.drop_dead_consultant_controls
# Before after_migrate, which is what saves doctypes and so what trips over a
# DocPerm granting submit without write. Position among the patches above does
# not matter -- none of them writes permissions.
work_management.patches.v1_0.repair_submit_without_write
work_management.patches.v1_0.show_task_subjects_in_links
work_management.patches.v1_0.backfill_actuals_hours
work_management.patches.v1_0.switch_on_the_new_discrepancy_checks
# Same trap, one field later: a Check added to an existing Single arrives 0
# whatever its default says. Beside its predecessor because it is the same fix
# for the same reason, and neither depends on anything.
work_management.patches.v1_0.switch_on_the_closed_short_check
# Employee.custom_basic_pay and its week stamp: CORE_CUSTOM_FIELDS is only
# applied by after_install, so a field added to it later needs this to reach a
# site that is already up.
work_management.patches.v1_0.add_the_basic_pay_field
# After the app's own code is in place: the rule it applies lives in
# work_management/consultant.py, and the patch only repairs what is already on
# the site. No-ops where the consultant step is switched on.
work_management.patches.v1_0.settle_activities_with_no_consultant_step
# Before anybody renames a step again: it keys approver rows by the name they
# carry, and every rename made first is a row it may not recover. Nothing above
# depends on it; after_migrate's apply_stage_picker_options() then offers keys.
work_management.patches.v1_0.key_stage_approvers_by_stage
# After key_stage_approvers_by_stage, and last: it writes the catalogue's state
# and action onto the stage rows, moves documents waiting under the old names
# and rebuilds the workflows, all in one transaction, before after_migrate. A
# patch above that rebuilds the workflows only ever runs on a site that has not
# had it yet -- live Altura has had every one of them.
work_management.patches.v1_0.rescue_documents_in_renamed_states
